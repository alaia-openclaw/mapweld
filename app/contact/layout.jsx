export const metadata = {
  title: "Contact",
  description:
    "Get in touch with the MapWeld team — questions, feature requests, or bug reports.",
  alternates: { canonical: "https://www.mapweld.app/contact" },
};

export default function ContactLayout({ children }) {
  return children;
}
