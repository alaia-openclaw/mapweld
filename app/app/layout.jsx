export const metadata = {
  title: "Weld Tracker",
  description:
    "Track welds on PDF drawings — place markers, update statuses, and export to Excel.",
  robots: { index: false, follow: false },
};

export default function AppLayout({ children }) {
  return children;
}
